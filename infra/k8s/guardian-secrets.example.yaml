apiVersion: v1
kind: Secret
metadata:
  name: fortressai-secrets
  namespace: fortressai
type: Opaque
stringData:
  JWT_SECRET_KEY: "replace-with-strong-secret"
  ETHERSCAN_API_KEY: "replace-with-etherscan-key"
  ELASTIC_INGEST_URL: "https://your-elastic-endpoint/security-events"
